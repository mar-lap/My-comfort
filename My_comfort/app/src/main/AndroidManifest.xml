<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mycomfort">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyComfort">
        <activity android:name=".activities.Books"></activity>
        <activity android:name=".activities.TheoryList" />
        <activity android:name=".activities.RorschachTest" />
        <activity android:name=".activities.PsyServices" />
        <activity android:name=".activities.PracticeText" />
        <activity android:name=".activities.PracticeList" />
        <activity android:name=".activities.ArticleText" />
        <activity android:name=".activities.MainWindow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>